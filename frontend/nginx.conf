server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Try to serve files directly, fallback to index.html for SPA routing
        try_files $uri $uri/ /index.html;
    }

    # Optionally, you can add error handling, gzip, etc.
}
